# ApiClientApp

This project is a simple C# console application for making API calls.

## What it does
- Sends a sample GET request
- Sends a sample POST request
- Uses HttpClient and JSON serialization

## Run it
From this folder:

```bash
 dotnet run --project ApiClientApp.csproj
```

You can update Program.cs to call your own API endpoints.
